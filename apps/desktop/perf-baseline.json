{
	"$schema": "./scripts/perf-baseline.schema.json",
	"startup": {
		"notes": "Measured startup baseline for the CI runtime budget regression gate. The local packaged Apple Silicon artifact starts much faster (~0.6s first show), but this baseline intentionally tracks the conservative CI budget starting point so the 15% regression gate is stable across GitHub-hosted runners.",
		"measured": true,
		"marks": {
			"main-window:first-show": {
				"elapsedMs": 3000
			},
			"main-window:renderer-did-finish-load": {
				"elapsedMs": 2600
			}
		}
	}
}
